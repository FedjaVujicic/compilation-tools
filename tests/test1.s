.section code1
add %r1, %r2
add %r1, %r2
add %r1, %r2

.section code2
add %r1, %r2
add %r1, %r2
add %r1, %r2

.section code3
add %r1, %r2
add %r1, %r2
add %r1, %r2

.section code4
add %r1, %r2
add %r1, %r2
add %r1, %r2